// Simple wrapper - using native smooth scroll via CSS
// Lenis was causing issues, so we're using browser-native scrolling

export default function SmoothScroll({ children }) {
    return <>{children}</>
}
